experiment:
  name: "baseline_monolithic_run_b"
  architecture: "monolithic" 
  worker_count: 1
  task_type: "B" 

workload:
  chunk_count: 5
  dataset_path: "./data/dummy_task_b.json"

simulation:
  mock_inference_latency_ms: 100
  timeout_threshold_ms: 5000
  retry_policy:
    enabled: true
    max_retries: 3

failure_injection:
  mode: "none"
  target_worker_id: null
  timing_ms: 0
  straggler_delay_ms: 0
